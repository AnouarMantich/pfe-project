package org.app.consultationservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MetricsConfig {
    // Metrics configuration will be added when actuator dependencies are properly configured
    // For now, we'll use simple logging-based metrics
}
